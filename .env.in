# template .env file
# follows the structure of the `Settings` class in `depositduck.settings`

APP_NAME=DepositDuck
# important! must be a Fernet key - see README for instructions to generate one
APP_SECRET="changeme_fernet_key"
APP_ORIGIN=https://depositduck.tld  # 0.0.0.0:8000 for local development

DEBUG=true

DB_USER=depositduck
DB_PASSWORD=password
DB_NAME=depositduck
DB_HOST=localhost
DB_PORT=5432

SMTP_SERVER=https://transactional.mail.example  # 0.0.0.0 for local development
SMTP_PORT=465  # 1025 for local development
SMTP_USE_SSL=true
SMTP_SENDER_ADDRESS=noreply@depositduck.local
SMTP_PASSWORD=smtp_password

DRALLAM_ORIGIN=http://localhost:11434

STATIC_ORIGIN=https://bucket.hosting.example/static
SPECULUM_RELEASE=speculum@M.m.p
